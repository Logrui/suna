---
description: Research target/existing repo and create a standalone codemap.
---

## User Input
```text
$ARGUMENTS
```

## Goal
To generate a deep technical map (`codemap.md`) in the root for exploration purposes (without triggering a full port).

## Execution Steps
1.  **Analyze**: Run `map-dependencies` script on target.
2.  **Draft**:
    *   Create `codemap.md`.
    *   Add File Tree, Critical Services, Mermaid Diagrams.
3.  **Report**: "Codemap generated at `./codemap.md`."
